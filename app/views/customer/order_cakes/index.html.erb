<div class="row row-top">
    <h3 style="padding-left:150px;">オーダーケーキ(全<%= @order_cakes.count %>件)</h3>
    <div class="col-xs-3 col-top">

      <%= render 'customer/search/search_form' %>

      <table class="table table-bordered genre-table">
        <thead>
          <tr>
            <th>
              ジャンル検索
            </th>
          </tr>
        </thead>
        <tbody>
          <% @genres.each do |genre| %>
          <tr>
            <td><%= link_to genre.name, search_path('search[value]': genre.id, 'search[how]': "match") %></td>
          </tr>
          <% end %>
        </tbody>
      </table>
    </div>
    <div class="col-xs-9 col-top">
      <% if @order_cakes.empty? %>
        <h4>現在注文可能なオーダーケーキはございません。</h4>
        <p>
          オリジナルケーキのご依頼は
          <%= link_to "こちら", new_order_cake_path, class:"order-link", 'data-turbolinks': false %>からお願いします。
        </p>
    　<% end %>
      <% @order_cakes.each do |order_cake| %>
        <div class="box" style="float:left; margin-right:20px;">
          <%= link_to item_path(order_cake.id) do %>
            <%= attachment_image_tag order_cake, :image, size: "190x150", class: "img_hover" %><br>
          <% end %>
          <p class="fas fa-chevron-circle-right item-name"><%=  order_cake.name %></p><br>
          <p class="fas fa-chevron-circle-right">￥<%= order_cake.price_without_tax %></p>
        </div>
      <% end %>
    </div>
  </div>